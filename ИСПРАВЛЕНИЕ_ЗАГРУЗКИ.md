# 🔧 Исправление: Загрузить все файлы на GitHub

## Проблема
На GitHub появился только README, остальные файлы не загрузились.

---

## ✅ Решение

### Шаг 1: Проверьте текущий статус

Откройте PowerShell в папке проекта и выполните:

```powershell
cd "C:\Users\ALFA\Desktop\active bot"
git status
```

**Что вы должны увидеть:**
- Если видите список файлов с "Untracked files" - файлы не добавлены
- Если видите "nothing to commit" - все уже добавлено

---

### Шаг 2: Добавьте ВСЕ файлы

```powershell
git add .
```

Эта команда добавит все файлы (кроме тех, что в .gitignore)

---

### Шаг 3: Проверьте, что файлы добавлены

```powershell
git status
```

Теперь вы должны увидеть список файлов, готовых к коммиту (зеленым цветом).

**Убедитесь, что `.env` НЕ в списке!** Если видите `.env` - НЕ коммитьте его!

---

### Шаг 4: Создайте коммит

```powershell
git commit -m "Add all bot files"
```

---

### Шаг 5: Загрузите на GitHub

```powershell
git push -u origin main
```

Если попросит логин/пароль:
- **Username:** ваш GitHub username
- **Password:** используйте Personal Access Token (не обычный пароль!)

---

## 🚨 Если репозиторий уже создан на GitHub с README

Если вы создали репозиторий через веб-интерфейс GitHub и там уже есть README, нужно сначала "подтянуть" его:

```powershell
# Подключите удаленный репозиторий (если еще не подключен)
git remote add origin https://github.com/ВАШ_USERNAME/discord-status-bot.git

# Подтяните README с GitHub
git pull origin main --allow-unrelated-histories

# Добавьте все ваши файлы
git add .

# Создайте коммит
git commit -m "Add all bot files"

# Загрузите все на GitHub
git push -u origin main
```

---

## 🔍 Проверка после загрузки

1. Откройте ваш репозиторий на GitHub
2. Проверьте, что видны все файлы:
   - `bot.py`
   - `bot_replit.py`
   - `keep_alive.py`
   - `requirements.txt`
   - и т.д.

3. **Убедитесь, что `.env` НЕТ в списке файлов!**

---

## ⚠️ Если видите `.env` на GitHub

**НЕМЕДЛЕННО:**

1. **Сбросьте токен бота:**
   - [discord.com/developers](https://discord.com/developers/applications)
   - Ваше приложение → Bot → Reset Token

2. **Удалите `.env` из Git:**
   ```powershell
   git rm --cached .env
   git commit -m "Remove .env file"
   git push
   ```

3. **Добавьте `.env` в `.gitignore`** (если еще не добавлен)

---

## 📋 Полная последовательность команд (если все заново)

```powershell
# Перейдите в папку
cd "C:\Users\ALFA\Desktop\active bot"

# Инициализация (если еще не сделано)
git init

# Проверьте .gitignore
type .gitignore
# Должен содержать: .env

# Добавьте все файлы
git add .

# Проверьте статус (убедитесь что .env НЕТ в списке!)
git status

# Создайте коммит
git commit -m "Initial commit: Discord status bot"

# Подключите к GitHub (замените URL!)
git remote add origin https://github.com/ВАШ_USERNAME/discord-status-bot.git

# Если репозиторий уже существует с README
git pull origin main --allow-unrelated-histories

# Загрузите на GitHub
git branch -M main
git push -u origin main
```

---

## 🆘 Проблемы?

**"fatal: not a git repository"**
- Выполните `git init` сначала

**"fatal: remote origin already exists"**
- Удалите старый: `git remote remove origin`
- Добавьте заново: `git remote add origin https://github.com/...`

**"error: failed to push"**
- Проверьте URL репозитория
- Убедитесь, что используете Personal Access Token как пароль

**Файлы все еще не загружаются**
- Проверьте `git status` - какие файлы видны?
- Убедитесь, что файлы не в `.gitignore`
