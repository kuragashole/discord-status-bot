# 🚀 Быстрый старт на Replit (5 минут)

## Шаг 1: Создайте аккаунт
1. Перейдите на [replit.com](https://replit.com)
2. Зарегистрируйтесь (можно через GitHub)

## Шаг 2: Создайте новый Repl
1. Нажмите **"Create Repl"**
2. Выберите **"Python"**
3. Назовите проект: `discord-status-bot`

## Шаг 3: Загрузите файлы
**Вариант A: Через GitHub (рекомендуется)**
1. Загрузите код на GitHub
2. В Replit: **Import from GitHub**
3. Выберите ваш репозиторий

**Вариант B: Вручную**
1. Скопируйте все файлы проекта в Replit
2. Убедитесь, что есть:
   - `bot_replit.py` (используйте этот файл!)
   - `keep_alive.py`
   - `requirements.txt`
   - Все остальные файлы

## Шаг 4: Настройте переменные окружения
1. В Replit откройте вкладку **"Secrets"** (🔒 слева)
2. Добавьте переменные (нажмите **"New secret"** для каждой):

```
DISCORD_TOKEN = ваш_токен_бота
OWNER_ID = ваш_discord_id
IMAGE_URL = путь_к_гифке_work_или_url
IMAGE_URL_SLEEP = путь_к_гифке_sleep_или_url
TICKET_CHANNEL_ID = id_канала_тикетов (или 0)
STATUS_LOG_CHANNEL_ID = id_канала_логов (или 0)
```

**Пример (ЗАМЕНИТЕ на свои значения!):**
```
DISCORD_TOKEN = ваш_реальный_токен_здесь
OWNER_ID = ваш_реальный_discord_id
IMAGE_URL = https://i.imgur.com/example.gif
IMAGE_URL_SLEEP = https://i.imgur.com/example2.gif
TICKET_CHANNEL_ID = 0
STATUS_LOG_CHANNEL_ID = 0
```

⚠️ **ВАЖНО:** Используйте РЕАЛЬНЫЕ значения, не копируйте примеры!

## Шаг 5: Установите зависимости
1. В консоли Replit выполните:
```bash
pip install -r requirements.txt
```

## Шаг 6: Запустите бота
1. В Replit нажмите кнопку **"Run"** ▶️
2. Или в консоли:
```bash
python bot_replit.py
```

## ✅ Готово!
Бот запущен и работает 24/7!

**Проверка:**
- В консоли должно быть: `✅ Keep-alive server is running on port 8080`
- Бот должен подключиться к Discord
- Команды `/setup_status` и `/start` должны появиться в Discord через 1-2 минуты

---

## 🔧 Если бот засыпает

Replit может засыпать после неактивности. Решения:

1. **Используйте `bot_replit.py`** - он уже включает keep-alive
2. **Настройте UptimeRobot** (опционально):
   - Создайте аккаунт на [uptimerobot.com](https://uptimerobot.com)
   - Добавьте монитор для вашего Repl URL
   - Интервал: 5 минут

---

## 📝 Важные замечания

1. **Используйте `bot_replit.py`**, а не `bot.py` на Replit!
2. **Не публикуйте токен** - используйте Secrets
3. **Для GIF файлов** лучше использовать URL (imgur, discord CDN)
4. **Локальные файлы** можно загрузить в Replit через файловый менеджер

---

## 🆘 Проблемы?

**Бот не запускается:**
- Проверьте, что все переменные в Secrets установлены
- Проверьте токен бота
- Посмотрите логи в консоли

**Команды не появляются:**
- Подождите 1-2 минуты
- Запустите `sync_commands.py` (если есть)
- Проверьте права бота на сервере

**Бот засыпает:**
- Убедитесь, что используете `bot_replit.py`
- Проверьте, что keep-alive сервер запущен (порт 8080)
