# 🔒 Безопасная загрузка кода на GitHub

## ⚠️ ВАЖНО: Защита токена бота

**Токен бота НИКОГДА не должен попасть в GitHub!**

✅ **Что уже защищено:**
- `.env` файл в `.gitignore` - не будет загружен
- `status_messages.json` в `.gitignore` - не будет загружен
- В коде нет хардкод токенов - все через переменные окружения

---

## 📝 Пошаговая инструкция

### Шаг 1: Создайте приватный репозиторий на GitHub

1. **Войдите на [github.com](https://github.com)**
   - Создайте аккаунт, если его нет

2. **Создайте новый репозиторий:**
   - Нажмите **"+"** в правом верхнем углу → **"New repository"**
   - **Repository name:** `discord-status-bot` (или любое другое имя)
   - **Description:** `Discord status bot with work/sleep modes`
   - ⚠️ **ВАЖНО:** Выберите **"Private"** (приватный)!
   - ❌ **НЕ** ставьте галочку "Add a README file" (у нас уже есть файлы)
   - Нажмите **"Create repository"**

---

### Шаг 2: Установите Git (если еще не установлен)

**Проверьте, установлен ли Git:**
```bash
git --version
```

**Если Git не установлен:**
1. Скачайте с [git-scm.com](https://git-scm.com/download/win)
2. Установите с настройками по умолчанию

---

### Шаг 3: Инициализируйте Git в папке проекта

1. **Откройте PowerShell или CMD в папке проекта:**
   ```bash
   cd "C:\Users\ALFA\Desktop\active bot"
   ```

2. **Инициализируйте Git:**
   ```bash
   git init
   ```

3. **Проверьте .gitignore:**
   ```bash
   type .gitignore
   ```
   
   Должно быть:
   ```
   .env
   __pycache__/
   *.pyc
   ...
   status_messages.json
   ```

---

### Шаг 4: Добавьте файлы в Git

**Проверьте, что .env НЕ будет добавлен:**
```bash
git status
```

Вы НЕ должны видеть `.env` в списке файлов!

**Добавьте все файлы (кроме .env):**
```bash
git add .
```

**Проверьте еще раз:**
```bash
git status
```

Убедитесь, что `.env` НЕТ в списке!

---

### Шаг 5: Создайте первый коммит

```bash
git commit -m "Initial commit: Discord status bot"
```

---

### Шаг 6: Подключите к GitHub

1. **Скопируйте URL вашего репозитория:**
   - На странице репозитория нажмите зеленую кнопку **"Code"**
   - Скопируйте URL (например: `https://github.com/ваш_username/discord-status-bot.git`)

2. **Добавьте remote:**
   ```bash
   git remote add origin https://github.com/ваш_username/discord-status-bot.git
   ```
   (Замените на ваш URL!)

3. **Переименуйте ветку в main (если нужно):**
   ```bash
   git branch -M main
   ```

4. **Загрузите код:**
   ```bash
   git push -u origin main
   ```

5. **Введите логин и пароль GitHub:**
   - Если попросит логин - введите ваш GitHub username
   - Если попросит пароль - используйте **Personal Access Token** (см. ниже)

---

### Шаг 7: Создайте Personal Access Token (если нужно)

Если Git просит пароль, используйте токен:

1. **GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)**
2. Нажмите **"Generate new token"**
3. Название: `Git Push Token`
4. Выберите срок действия (например, 90 дней)
5. Отметьте галочку **`repo`** (полный доступ к репозиториям)
6. Нажмите **"Generate token"**
7. **Скопируйте токен** (он показывается только один раз!)
8. Используйте этот токен как пароль при `git push`

---

## ✅ Проверка безопасности

После загрузки проверьте:

1. **Откройте ваш репозиторий на GitHub**
2. **Проверьте, что репозиторий ПРИВАТНЫЙ:**
   - Должна быть метка "Private" рядом с названием
3. **Проверьте, что .env НЕ загружен:**
   - В списке файлов НЕ должно быть `.env`
   - Если видите `.env` - **НЕМЕДЛЕННО удалите его!** (см. ниже)

---

## 🚨 Если случайно загрузили .env

**Если вы случайно загрузили .env в GitHub:**

1. **НЕМЕДЛЕННО сбросьте токен бота:**
   - Перейдите на [discord.com/developers](https://discord.com/developers/applications)
   - Ваше приложение → Bot → Reset Token
   - Создайте новый токен

2. **Удалите .env из истории Git:**
   ```bash
   git rm --cached .env
   git commit -m "Remove .env file"
   git push
   ```

3. **Очистите историю (опционально, но рекомендуется):**
   - GitHub → Settings → Security → Delete repository
   - Создайте новый репозиторий и загрузите код заново

---

## 📋 Файлы, которые БУДУТ загружены:

✅ **Безопасные файлы (можно загружать):**
- `bot.py`
- `bot_replit.py`
- `keep_alive.py`
- `sync_commands.py`
- `clear_commands.py`
- `requirements.txt`
- `README.md`
- `ИНСТРУКЦИЯ.md`
- `ХОСТИНГ.md`
- `QUICK_START_REPLIT.md`
- `GITHUB_SETUP.md`
- `render.yaml`
- `fly.toml`
- `.gitignore`

❌ **Файлы, которые НЕ будут загружены (защищены .gitignore):**
- `.env` ⚠️ **СОДЕРЖИТ ТОКЕН!**
- `status_messages.json`
- `__pycache__/`
- `*.pyc`

---

## 🔐 Дополнительная безопасность

### Сделайте репозиторий приватным (если еще не сделали):

1. GitHub → Ваш репозиторий → Settings
2. Scroll down → Danger Zone
3. Нажмите **"Change visibility"**
4. Выберите **"Make private"**

### Проверьте настройки доступа:

1. GitHub → Ваш репозиторий → Settings → Collaborators
2. Убедитесь, что нет лишних людей с доступом

---

## ✅ Готово!

Теперь ваш код безопасно на GitHub в приватном репозитории!

**Следующий шаг:** Подключите Replit к GitHub (см. QUICK_START_REPLIT.md)

---

## 🆘 Проблемы?

**Git не найден:**
- Установите Git с [git-scm.com](https://git-scm.com/download/win)

**Ошибка при push:**
- Проверьте URL репозитория
- Убедитесь, что используете Personal Access Token как пароль

**Файлы не загружаются:**
- Проверьте `git status`
- Убедитесь, что файлы добавлены через `git add .`
