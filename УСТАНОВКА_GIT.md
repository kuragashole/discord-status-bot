# 📥 Установка Git на Windows

## Проблема
Ошибка: `git : Имя "git" не распознано...`

Это означает, что Git не установлен на вашем компьютере.

---

## ✅ Решение: Установить Git

### Вариант 1: Через официальный установщик (рекомендуется)

1. **Скачайте Git:**
   - Перейдите на [git-scm.com/download/win](https://git-scm.com/download/win)
   - Скачайте установщик (автоматически определит 64-bit или 32-bit)

2. **Установите Git:**
   - Запустите скачанный файл (например, `Git-2.43.0-64-bit.exe`)
   - Нажимайте **"Next"** на всех шагах (настройки по умолчанию подойдут)
   - ⚠️ **Важно:** Оставьте галочку **"Git from the command line and also from 3rd-party software"** (должна быть по умолчанию)
   - Нажмите **"Install"**
   - Дождитесь завершения установки

3. **Перезапустите PowerShell:**
   - Закройте текущее окно PowerShell
   - Откройте новое окно PowerShell
   - Перейдите в папку проекта:
     ```powershell
     cd "C:\Users\ALFA\Desktop\active bot"
     ```

4. **Проверьте установку:**
   ```powershell
   git --version
   ```
   
   Должно показать что-то вроде: `git version 2.43.0.windows.1`

---

### Вариант 2: Через winget (если установлен)

```powershell
winget install --id Git.Git -e --source winget
```

---

### Вариант 3: Через GitHub Desktop (проще для начинающих)

Если не хотите работать с командной строкой:

1. **Скачайте GitHub Desktop:**
   - [desktop.github.com](https://desktop.github.com)
   - Установите программу

2. **Используйте GitHub Desktop:**
   - Откройте GitHub Desktop
   - File → Add Local Repository
   - Выберите папку `C:\Users\ALFA\Desktop\active bot`
   - Создайте репозиторий через интерфейс
   - Нажмите "Publish repository" (сделайте приватным!)

---

## ✅ После установки Git

Когда Git установлен, выполните команды из `БЫСТРЫЙ_СТАРТ.md`:

```powershell
# Перейдите в папку проекта
cd "C:\Users\ALFA\Desktop\active bot"

# Инициализация
git init
git add .
git commit -m "Initial commit"

# Подключение к GitHub (замените URL!)
git remote add origin https://github.com/ВАШ_USERNAME/discord-status-bot.git
git branch -M main
git push -u origin main
```

---

## 🆘 Проблемы?

**Git все еще не работает после установки:**
- Закройте и откройте PowerShell заново
- Или перезагрузите компьютер

**Не могу скачать Git:**
- Попробуйте альтернативный способ: GitHub Desktop (см. выше)

**Ошибка при установке:**
- Убедитесь, что у вас есть права администратора
- Попробуйте запустить установщик от имени администратора

---

## 📝 Следующие шаги

После установки Git:
1. Следуйте инструкции из `GITHUB_SETUP.md`
2. Или используйте быстрый старт из `БЫСТРЫЙ_СТАРТ.md`
