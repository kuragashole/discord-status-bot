# 📘 Полная инструкция по запуску Discord бота

## Шаг 1: Установка Python

1. Скачайте Python с официального сайта: https://www.python.org/downloads/
2. При установке **обязательно** отметьте галочку "Add Python to PATH"
3. Проверьте установку: откройте командную строку (Win+R → cmd) и введите:
   ```
   python --version
   ```
   Должна отобразиться версия Python (например, Python 3.11.0)

---

## Шаг 2: Создание Discord бота

### 2.1. Создание приложения

1. Перейдите на сайт: https://discord.com/developers/applications
2. Нажмите кнопку **"New Application"** (Новое приложение)
3. Введите название бота (например, "Status Bot") и нажмите **"Create"**

### 2.2. Создание бота

1. В левом меню выберите **"Bot"**
2. Нажмите **"Add Bot"** → **"Yes, do it!"**
3. В разделе **"Token"** нажмите **"Reset Token"** → **"Yes, do it!"**
4. **Скопируйте токен** (это длинная строка букв и цифр) - **СОХРАНИТЕ ЕГО!** Он понадобится позже
   - ⚠️ **ВАЖНО:** Никому не показывайте этот токен!

### 2.3. Настройка прав бота

1. В разделе **"Bot"** прокрутите вниз до **"Privileged Gateway Intents"**
2. Включите следующие опции:
   - ✅ **MESSAGE CONTENT INTENT** (обязательно!)
   - ✅ **SERVER MEMBERS INTENT** (опционально, но рекомендуется)

### 2.4. Приглашение бота на сервер

1. В левом меню выберите **"OAuth2"** → **"URL Generator"**
2. В разделе **"Scopes"** отметьте:
   - ✅ **bot**
   - ✅ **applications.commands**
3. В разделе **"Bot Permissions"** отметьте:
   - ✅ **Send Messages** (Отправлять сообщения)
   - ✅ **Embed Links** (Встраивать ссылки)
   - ✅ **Read Message History** (Читать историю сообщений)
   - ✅ **Use Slash Commands** (Использовать слэш-команды)
4. Внизу страницы скопируйте **"Generated URL"**
5. Откройте эту ссылку в браузере
6. Выберите ваш сервер и нажмите **"Authorize"**
7. Пройдите проверку (если потребуется)

---

## Шаг 3: Получение вашего Discord ID

1. Откройте Discord
2. Перейдите в **Настройки** (шестеренка внизу слева)
3. Перейдите в **Дополнительно** → **Режим разработчика** → включите
4. Вернитесь на сервер
5. **Правой кнопкой мыши** по вашему аватару/имени → **"Копировать ID"**
6. Сохраните этот ID (это число, например: 123456789012345678)

---

## Шаг 4: Подготовка изображения (опционально)

Вы можете использовать:
- **Локальный файл:** Поместите изображение в папку с ботом (например, `status.png` или `status.gif`)
- **URL ссылку:** Используйте прямую ссылку на изображение (например, с imgur.com, discord CDN)

**Рекомендации:**
- **Форматы:** PNG, JPG, GIF (анимированные GIF тоже поддерживаются! 🎉)
- **Размер:** до 8MB
- **Для статичных изображений:** PNG для лучшего качества
- **Для анимации:** GIF через URL (рекомендуется) или локальный файл
- **GIF через URL:** Лучше всего использовать прямую ссылку на GIF (например, `https://i.imgur.com/example.gif`)

**Примеры:**
- Локальный GIF: `IMAGE_URL=./status.gif`
- URL GIF: `IMAGE_URL=https://i.imgur.com/example.gif`
- Локальный PNG: `IMAGE_URL=./status.png`
- URL изображения: `IMAGE_URL=https://i.imgur.com/example.png`

---

## Шаг 5: Установка зависимостей

1. Откройте командную строку в папке с ботом:
   - Нажмите `Shift + Правый клик` в папке → **"Открыть окно PowerShell здесь"**
   - Или откройте командную строку и перейдите в папку:
     ```
     cd "C:\Users\ALFA\Desktop\active bot"
     ```

2. Установите зависимости:
   ```
   pip install -r requirements.txt
   ```

   Если команда не работает, попробуйте:
   ```
   python -m pip install -r requirements.txt
   ```

---

## Шаг 6: Создание файла .env

1. В папке с ботом создайте файл с именем `.env` (без расширения!)
   - Можно использовать Блокнот (Notepad)
   - При сохранении выберите **"Все файлы"** и укажите имя `.env`

2. Откройте файл `.env` и вставьте следующее:

```
DISCORD_TOKEN=ваш_токен_бота_здесь
OWNER_ID=ваш_discord_id_здесь
IMAGE_URL=https://i.imgur.com/example.png
TICKET_CHANNEL_ID=0
```

3. Замените значения:
   - `ваш_токен_бота_здесь` → токен из шага 2.2
   - `ваш_discord_id_здесь` → ваш ID из шага 3
   - `https://i.imgur.com/example.png` → URL вашего изображения или путь к файлу (например, `./status.png`)
   - `TICKET_CHANNEL_ID=0` → оставьте 0 или укажите ID канала для тикетов

**Пример готового .env файла:**
```
DISCORD_TOKEN=MTIzNDU2Nzg5MDEyMzQ1Njc4OQ.GaBcDe.EfGhIjKlMnOpQrStUvWxYzAbCdEfGhIjKlMnOpQr
OWNER_ID=123456789012345678
IMAGE_URL=./status.png
TICKET_CHANNEL_ID=0
```

---

## Шаг 7: Запуск бота

1. В командной строке (в папке с ботом) выполните:
   ```
   python bot.py
   ```

2. Если все настроено правильно, вы увидите:
   ```
   Status Bot подключен к Discord!
   Синхронизировано 2 команд
   ```

3. Бот запущен! ✅

---

## Шаг 8: Использование бота

### В Discord:

1. **Создание статусного сообщения:**
   - Перейдите в канал, где хотите создать статус
   - Введите: `/setup_status`
   - Нажмите Enter
   - Бот создаст сообщение со статусом

2. **Изменение статуса:**
   - `/start work` - установить статус "Active" (зеленый 🟢)
   - `/start sleep` - установить статус "Sleep" (красный 🔴) с кнопкой тикета

### Важно:
- Команды видны и доступны **только вам** (владельцу бота)
- Другие пользователи видят только кнопку "Создать тикет" при статусе Sleep

---

## 🔧 Решение проблем

### Ошибка: "DISCORD_TOKEN не найден"
- Проверьте, что файл `.env` создан правильно
- Убедитесь, что токен скопирован полностью (без пробелов)
- Файл должен называться именно `.env` (не `.env.txt`)

### Ошибка: "OWNER_ID не установлен"
- Проверьте, что ваш Discord ID указан в `.env`
- Убедитесь, что режим разработчика включен в Discord

### Бот не отвечает на команды
- Убедитесь, что бот онлайн (зеленый индикатор в Discord)
- Проверьте, что включен **MESSAGE CONTENT INTENT** в настройках бота
- Подождите несколько минут после первого запуска (команды синхронизируются)

### Команды не видны
- Подождите 1-2 минуты после запуска бота
- Перезапустите Discord
- Проверьте, что бот добавлен на сервер с правами **applications.commands**

### Изображение не отображается
- Проверьте URL изображения (откройте в браузере)
- Для локальных файлов убедитесь, что путь указан правильно
- Размер файла не должен превышать 8MB

---

## 📝 Дополнительные настройки

### Настройка канала для тикетов

1. Получите ID канала:
   - Включите режим разработчика в Discord
   - Правый клик по каналу → **"Копировать ID"**

2. Добавьте в `.env`:
   ```
   TICKET_CHANNEL_ID=123456789012345678
   ```

3. Перезапустите бота

---

## 🛑 Остановка бота

Для остановки бота нажмите `Ctrl + C` в командной строке

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте все шаги инструкции
2. Убедитесь, что все значения в `.env` указаны правильно
3. Проверьте, что бот онлайн в Discord
4. Посмотрите сообщения об ошибках в командной строке

---

**Удачи с вашим ботом! 🚀**
