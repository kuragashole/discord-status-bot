# 🚀 Бесплатный хостинг Discord бота 24/7

## 📋 Варианты бесплатного хостинга (для России)

### ✅ **Вариант 1: Replit** (Самый простой) ⭐ РЕКОМЕНДУЮ

**Плюсы:**
- Полностью бесплатно
- Простая настройка
- Работает из браузера
- Не требует карты

**Минусы:**
- Может засыпать после неактивности (решается keep-alive)

**Инструкция:**

1. **Создайте аккаунт на [Replit.com](https://replit.com)**
   - Зарегистрируйтесь через GitHub или email

2. **Создайте новый Repl:**
   - Нажмите "Create Repl"
   - Выберите "Python" как язык
   - Назовите проект (например, "discord-status-bot")

3. **Загрузите файлы:**
   - Скопируйте все файлы проекта в Repl
   - Или подключите GitHub репозиторий

4. **Настройте переменные окружения:**
   - В Replit откройте вкладку "Secrets" (🔒)
   - Добавьте все переменные из `.env`:
     - `DISCORD_TOKEN`
     - `OWNER_ID`
     - `IMAGE_URL`
     - `IMAGE_URL_SLEEP`
     - `TICKET_CHANNEL_ID` (опционально)
     - `STATUS_LOG_CHANNEL_ID` (опционально)

5. **Установите зависимости:**
   - В консоли Replit выполните: `pip install -r requirements.txt`

6. **Запустите бота:**
   - Нажмите кнопку "Run"
   - Бот будет работать постоянно!

**⚠️ Важно:** Replit может засыпать. Используйте файл `keep_alive.py` (см. ниже)

---

### ✅ **Вариант 2: Render** (Надежный)

**Плюсы:**
- Надежный хостинг
- Автоматический деплой из GitHub
- Не засыпает на бесплатном плане (но может)

**Минусы:**
- Требует GitHub репозиторий
- Может засыпать после 15 минут неактивности

**Инструкция:**

1. **Создайте GitHub репозиторий:**
   - Загрузите код на GitHub

2. **Создайте аккаунт на [Render.com](https://render.com)**
   - Войдите через GitHub

3. **Создайте новый Web Service:**
   - New → Web Service
   - Подключите ваш GitHub репозиторий
   - Настройки:
     - **Name:** discord-status-bot
     - **Environment:** Python 3
     - **Build Command:** `pip install -r requirements.txt`
     - **Start Command:** `python bot.py`

4. **Добавьте переменные окружения:**
   - В разделе "Environment" добавьте все переменные из `.env`

5. **Деплой:**
   - Нажмите "Create Web Service"
   - Бот запустится автоматически!

**⚠️ Для предотвращения засыпания:** Используйте `keep_alive.py` или настройте uptime monitor

---

### ✅ **Вариант 3: Railway** (Хороший баланс)

**Плюсы:**
- Простая настройка
- Автоматический деплой из GitHub
- $5 бесплатных кредитов в месяц (хватит для бота)

**Минусы:**
- Требует карту (но не списывает деньги, если не превысите лимит)

**Инструкция:**

1. **Создайте аккаунт на [Railway.app](https://railway.app)**
   - Войдите через GitHub

2. **Создайте новый проект:**
   - New Project → Deploy from GitHub repo
   - Выберите ваш репозиторий

3. **Настройте переменные:**
   - Settings → Variables
   - Добавьте все переменные из `.env`

4. **Настройте деплой:**
   - Railway автоматически определит Python
   - Убедитесь, что `requirements.txt` есть в репозитории

5. **Готово!** Бот запустится автоматически

---

### ✅ **Вариант 4: Fly.io** (Бесплатный план)

**Плюсы:**
- Бесплатный план с ограничениями
- Надежный хостинг
- Не засыпает

**Минусы:**
- Требует карту (но не списывает на бесплатном плане)

**Инструкция:**

1. **Установите Fly CLI:**
   ```bash
   # Windows (PowerShell)
   iwr https://fly.io/install.ps1 -useb | iex
   ```

2. **Создайте аккаунт на [Fly.io](https://fly.io)**
   - Войдите через GitHub

3. **Используйте готовый `fly.toml`** (см. файл в проекте)

4. **Деплой:**
   ```bash
   fly auth login
   fly launch
   fly secrets set DISCORD_TOKEN=ваш_токен
   fly secrets set OWNER_ID=ваш_id
   # ... и т.д.
   ```

---

## 🔧 Keep-Alive для Replit (предотвращение засыпания)

Если используете Replit, добавьте файл `keep_alive.py`:

```python
from flask import Flask
from threading import Thread

app = Flask('')

@app.route('/')
def home():
    return "Bot is alive!"

def run():
    app.run(host='0.0.0.0', port=8080)

def keep_alive():
    t = Thread(target=run)
    t.daemon = True
    t.start()
```

И добавьте в начало `bot.py`:
```python
from keep_alive import keep_alive
keep_alive()
```

Также добавьте `flask` в `requirements.txt`

---

## 📝 Рекомендации

**Для начала:** Используйте **Replit** - самый простой вариант
**Для надежности:** Используйте **Railway** или **Render**

---

## ⚠️ Важные замечания

1. **Не публикуйте токен бота!**
   - Используйте переменные окружения
   - Не коммитьте `.env` в GitHub

2. **Добавьте `.env` в `.gitignore`**

3. **Для GIF файлов:**
   - Используйте URL (imgur, discord CDN)
   - Или загрузите файлы в Replit/Render

4. **Мониторинг:**
   - Проверяйте логи бота
   - Настройте уведомления об ошибках

---

## 🆘 Помощь

Если бот не работает:
1. Проверьте логи в консоли хостинга
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что токен бота правильный
4. Убедитесь, что бот добавлен на сервер с правильными правами
